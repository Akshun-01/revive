# Revive backend base URL (FastAPI). The client appends /api/v1.
#
# Local backend:  http://127.0.0.1:8000
# Shared backend:  https://revive-api.log0.in   (stable named Cloudflare tunnel)
#
# For Vercel, set NEXT_PUBLIC_API_BASE in the project's Environment Variables. NEXT_PUBLIC_* is
# inlined at build time, so a change needs a redeploy. See docs/DEPLOY.md.
NEXT_PUBLIC_API_BASE=https://revive-api.log0.in

# Stub auth: user id sent as X-User-Id (and as a query param on SSE streams). Backend
# defaults to demo-user; keep in sync so connections and investigations share one user.
NEXT_PUBLIC_USER_ID=demo-user
